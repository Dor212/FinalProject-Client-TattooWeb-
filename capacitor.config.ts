import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dorohana.OmerAvivTattoo',
  appName: 'OmerAvivTattoo',
  webDir: 'dist'
};

export default config;
